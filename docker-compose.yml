version: "3"

services:
  backend:
    container_name: backend_container
    build: ./api
    ports:
      - "5000:5000"
    depends_on:
      - database
    environment:
      - MONGO_URI=mongodb://database:27017/?directConnection=true

  database:
    container_name: database_container
    image: mongo
    expose:
      - "27017"
    ports:
      - "27017:27017"

  frontend:
    container_name: frontend_container
    build:
      context: ./client/blog
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - database
      - backend